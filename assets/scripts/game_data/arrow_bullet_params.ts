interface BulletParams {
    speed: number;
    attack: number;
}

const arrow_bullet_params: BulletParams[] = [
    { // 等级1的子弹
        speed: 200,
        attack: 10,
    },
    
    { // 等级2的子弹
        speed: 200,
        attack: 10,
    },
    
    { // 等级3的子弹
        speed: 200,
        attack: 10,
    },
    
    { // 等级4的子弹
        speed: 200,
        attack: 10,
    },
];

export default arrow_bullet_params;
